\section{Algorithmic typing}
\label{sec:algorithmic-typing}
\subsection{Typing relation}

For the algorithmic typing we consider an extended grammar of session types
$\AS$ that includes unification variables $\TVar$ drawn from
$\TVAR$. We write $\AT$ for the set of types 
$\T$ but referring to $\AS$ instead of $\S$.

The algorithmic typing collects a set of constraints, denoted by
metavariables $\CSet$. Constraints come in three forms:
\begin{itemize}
  \item equality constraints between two types $\AT \CEq \ATU$
  \item mobility constraints $\CMobile \AT$
  \item boundedness constraints $\CBounded \AS$
\end{itemize}

\begin{definition}[Closing substitution]
  A substitution $\sigma : \TVAR \to \AS$ is a \emph{closing
    substitution} for  a set of constraints $\CSet$ if for every
  unification variable $\TVar$ in $\CSet$, $\sigma (\TVar) \in \S$ and all
  constraints are respected.
\end{definition}

\Cref{fig:typing-algorithmic} presents the algorithmic typing
judgments, which are based on bidirectional typing. The
synthesis judgment $\InferType \E \AT \Eff$ takes as input the context $\Ctx$
and expression $\E$. It computes the expression's type $\AT$, its effect
$\Eff$, and a set of constraints $\CSet$. The checking judgment $\CheckType \E
\AT \Eff$ takes as input the context $\Ctx$, expression $\E$, and type $\AT$. It
outputs the expression's effect $\Eff$ as well as a set of constraints $\CSet$.

\begin{definition}[Context join]
  The operator $\cdot \CtxJoin \cdot$ joins two contexts according to the
  direction $\Dir$.
  %
  \[ \Ctx[1] \CtxJoin \Ctx[2] = \begin{cases}
        \Ctx[1] \CtxPar \Ctx[2] & \text{if $\Dir \in \{ \DirUnord, \DirNone \}$} \\
        \Ctx[1] \CtxSeq \Ctx[2] & \text{if $\Dir = \DirLeft$} \\
        \Ctx[2] \CtxSeq \Ctx[1] & \text{if $\Dir = \DirRight$}
     \end{cases}
  \]
  %
\end{definition}

\begin{definition}[Context restriction]
The operator $\cdot|_{\cdot}$ restricts a context to a set of variables.
%
\[
  \CtxRestrict*{X} = \begin{cases}
    \CtxRestrict*[1]{X} \CtxPar \CtxRestrict*[2]{X} & \text{if $\Ctx=\Ctx[1] \CtxPar \Ctx[2]$} \\
    \CtxRestrict*[1]{X} \CtxSeq \CtxRestrict*[2]{X} & \text{if $\Ctx=\Ctx[1] \CtxSeq \Ctx[2]$} \\
    \CtxVar \EVar \T & \text{if $\Ctx=\CtxVar \EVar T$ and $\EVar \in X$} \\
    \CtxEmpty & \text{else}
  \end{cases}
\]
%
\end{definition}

\begin{figure}
  \small
  \hfill \fbox{$\InferType \E \T \Eff$}
  \begin{mathpar}
    \RuleAlgVar \and
    \RuleAlgConst \and
    \RuleAlgLSplit \and
    \RuleAlgRSplit \and
    \RuleAlgSelect \and
    \RuleAlgBranch \and
    \RuleAlgApp \and
    \RuleAlgSeq \and
    \RuleAlgLetPair \and
    \RuleAlgCase
  \end{mathpar}

  \vspace{1em}
  \hfill \fbox{$\CheckType \E \T \Eff$}
  \begin{mathpar}
    \RuleAlgAbs \and
    \RuleAlgAbsRec \and
    \RuleAlgPair \and
    \RuleAlgInj \and
    \RuleAlgCheck
  \end{mathpar}
  \caption{Algorithmic typing}
  \label{fig:typing-algorithmic}
\end{figure}

\begin{lemma}[Soundness]
  If\/ $\CheckType \E \AT \Eff$ and there is a closing substitution $\sigma$
  such that $\Fv(\AT) \subseteq \dom(\sigma)$ then $\HasType \Ctx \E {\AT{[\sigma]}}$.
\end{lemma}

\pt{what about the synthesis judgment?}

\begin{lemma}[Completeness]
  If\/ $\HasType \Ctx \E \T$ then $\CheckType \E \AT \Eff$ for some type $\AT$, set of
  constraints $\CSet$, and closing substitution $\sigma$ such that $\T=\AT{[\sigma]}$.
\end{lemma}


\subsection{Constraint solving}

Potential solutions can be checked by the equivalence algorithm for context
free session types. However, the literature does not provide unification
algorithms. \pt{This must be made more precise! If I read the
  wikipedia article correctly, then solutions can be enumerated by
  one-sided paramodulation; so the problem should be semi-decidable,
  at least.}
Our approach is based on the fact that unification variables are
only ever introduced for session types through split operations. Every session
type---excluding $\SSkip$ and its compositions---must be used linearly. Thus,
if a term with type $\TVar$ is not used the only viable solution for $\TVar$ is
$\SSkip$. Otherwise, we can infer the concrete type for $\TVar$ the terms use.
Multiple uses in different branches of the program must yield equivalent types.
If there are no solutions, the program is ill-typed.

\begin{mathpar}
  \inferrule[CS-Pair]{}{
    \AT \TPair \ATU \CEq \AT* \TPair \ATU*
    \csred
    \left\{
      \AT \CEq \AT*\!,
      \ATU \CEq \ATU*
    \right\}
  }

  \inferrule[CS-Sum]{}{
    \TSum {\AT} {\ATU} \CEq \TSum {\AT*} {\ATU*}
    \csred
    \left\{
      \AT \CEq \AT*\!,
      \ATU \CEq \ATU*
    \right\}
  }

  \inferrule[CS-Unit]{}{
    \BUnit \CEq \BUnit
    \csred
    \emptyset
  }

  \inferrule[CS-Arrow]{}{
    \TArr{\Mob}{\Dir,\Eff}{\AT}{\ATU}
    \CEq
    \TArr{\Mob}{\Dir,\Eff}{\AT*}{\ATU*}
    \csred
    \left\{
      \AT* \CEq \AT\!,
      \ATU \CEq \ATU*
    \right\}
  }

  \inferrule[CS-Ret]{}{
    \CBounded \SRet \csred \emptyset
  }

  \inferrule[CS-Close]{}{
    \CBounded \STerm \csred \emptyset
  }

  \inferrule[CS-Wait]{}{
    \CBounded \SWait \csred \emptyset
  }

  \inferrule[CS-Seq1]{
    \Skips \AS[2]
  }{
    \CBounded\ (\SSeq{\AS[1]}{\AS[2]}) \csred \left\{ \CBounded \AS[1] \right\}
  }

  \inferrule[CS-Seq2]{
    \neg \Skips \AS[2]
  }{
    \CBounded\ (\SSeq{\AS[1]}{\AS[2]}) \csred \left\{ \CBounded \AS[2] \right\}
  }

  \inferrule[CS-Acq]{}{
    \CMobile\ (\SSeq\SAcq\AS) \csred \left\{ \CBounded \AS \right\}
  }

  \inferrule[CS-MobileArrow]{}{
    \CMobile\ (\TArr{\MobMobile}{\Dir,\Eff}{\AT}{\ATU}) \csred \emptyset
  }

  \inferrule[CS-MobilePair]{}{
    \CMobile\ (\AT \TPair \ATU) \csred \left\{ \CMobile\AT, \CMobile\ATU \right\}
  }

  \inferrule[CS-Context]{
    \AT \CEq \ATU \csred \CSet*
  }{
    \CSet \cup \{ \AT\CEq\ATU \}
    \csred
    \CSet \cup \CSet*
  }
\end{mathpar}

$\Skips$ is decidable for unsolved session types because neither $\SSkip$ nor
any session type $\S$ such that $\Skips\S$ are solutions for unification
variables.


\subsection{Type Normalisation}

\begin{figure}
  \small
  \hfill \fbox{$\ConcatType {\T[in]} {\T[in]} = \T[out]$}
  \begin{mathpar}
    \TypeConcatenationSkip

    \TypeConcatenationSemiDist

    \TypeConcatenationNonSemi
  \end{mathpar}

  \hfill \fbox{$\NormaliseType {\T[in]} {\T[out]}$}
  \begin{mathpar}
    \TypeNormalisationSemiTerminated
    
    \TypeNormalisationSemiNonTerminated

    \TypeNormalisationMu

    \TypeNormalisationChoiceSemiDist

    \TypeNormalisationIdentity
  \end{mathpar}
  \caption{Type Normalisation}
  \label{fig:typing-normalisation}
\end{figure}

Type normalisation, denoted as $\NormaliseType {\T[in]} {\T[out]}$ in
\Cref{fig:typing-normalisation}, is used in the elimination rules for
algorithmic rules to expose the top-level type constructor
\cite{DBLP:journals/iandc/AlmeidaMTV22}.

\pt{Where is $\TerminatedType {\T}$ defined? Why is normalization terminating?}
\mw{if this is known work, skip this figure and refer to \cite{DBLP:journals/iandc/AlmeidaMTV22}}


%%% Local Variables:
%%% mode: latex
%%% TeX-master: "../popl2027.tex"
%%% End:
